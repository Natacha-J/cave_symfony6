<form method="post" action="{{ path('appellation_delete', {'codeappellation': appellation.codeappellation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ appellation.codeappellation) }}">
    <button class="btn">Delete</button>
</form>
