<form method="post" action="{{ path('pays_delete', {'codepays': pays.codepays}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce pays ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pays.codepays) }}">
    <button class="btn">Delete</button>
</form>
