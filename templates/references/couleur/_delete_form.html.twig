<form method="post" action="{{ path('couleur_delete', {'codecouleur': couleur.codecouleur}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ couleur.codecouleur) }}">
    <button class="btn">Delete</button>
</form>
